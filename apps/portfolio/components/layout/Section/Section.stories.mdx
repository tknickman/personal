import { Meta, Story } from "@storybook/addon-docs";
import isChromatic from "chromatic/isChromatic";

import Section from ".";

<Meta
  title="apps/portfolio/section"
  component={Section}
  parameters={{ chromatic: { diffThreshold: 0.2 } }}
/>

export const Template = (args) => <Section {...args} animate={!isChromatic} />;

<Story
  name="Default"
  args={{
    titleSolid: "Title",
    number: 1,
    children: "Cool",
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Center"
  args={{
    titleSolid: "Title",
    number: 2,
    children: "Cool",
    align: "center",
  }}
>
  {Template.bind({})}
</Story>
