import { Meta, Story } from "@storybook/addon-docs";

import Icon from "./Icon";
import { All_ICONS, CodePen } from ".";

<Meta title="@tek/icons" component={Icon} />

export const Template = (args) => (
  <div className="grid grid-cols-10 gap-4">
    {All_ICONS.map((I, idx) => (
      <div>
        <I key={idx} {...args} />
      </div>
    ))}
  </div>
);

<Story name="All Icons">{Template.bind({})}</Story>
